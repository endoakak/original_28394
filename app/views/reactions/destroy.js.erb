btnId = `<%= @reaction.comment.id %>`;
reactionButton = document.getElementById(`reaction-btn-${btnId}`);
checkedButton = document.getElementById(`reaction-btn-${btnId}-checked`);
reactionCount = document.getElementById(`reaction-count-${btnId}`);

reactionButton.setAttribute("class", "btn border-primary");
checkedButton.setAttribute("class", "hidden");
reactionCount.innerHTML = Number(reactionCount.innerHTML) - 1;
